package snake.app;

import snake.SnakeUI;
import javafx.application.Application;

public class SnakeApp {
    public static void main(String[] args) {
        Application.launch(SnakeUI.class, args);
    }
}